CREATE TABLE  "USUARIO" (
  "EMAIL" VARCHAR2(255) NOT NULL ENABLE PRIMARY KEY,
	"RUN" NUMBER(7) NOT NULL ENABLE,
	"PASSWORD" VARCHAR2(31) NOT NULL ENABLE,
	"NOMBRE" VARCHAR2(127) NOT NULL ENABLE,
	"ROL" NUMBER(8),
	"ESTILO" NUMBER(1),
)
ALTER TABLE "USUARIO"
  ADD PRIMARY KEY ("EMAIL");
