CREATE TABLE  "USUARIO" (
  "EMAIL" VARCHAR2(255) NOT NULL ENABLE,
	"RUN" NUMBER(7,0) NOT NULL ENABLE,
	"PASSWORD" VARCHAR2(31) NOT NULL ENABLE,
	"NOMBRE" VARCHAR2(127) NOT NULL ENABLE,
	"ROL" NUMBER(8,0),
	"ESTILO" NUMBER(1,0),
)
ALTER TABLE "USUARIO"
  ADD PRIMARY KEY ("EMAIL");
